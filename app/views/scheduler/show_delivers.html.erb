<%= calendar events: @meetings do |date, meetings| %>
  <%= l(date) %>
  <table class="table table-striped table-responsive">
    <thead>
      <th>Cliente</th>
      <th>Endereço</th>
      <th>Celular</th>
      <th>Tipo Pagamento</th>
      <th>Informações</th>
      <th>Horário</th>
    </thead>
    <% meetings.each do |meeting| %>
    <tbody>
      <tr>
        <td><%= meeting.name.capitalize %></td>
        <td><%= meeting.localization.capitalize %></td>
        <td><%= meeting.phone %></td>
        <td><%= meeting.payment_type.capitalize %></td>
        <td><%= meeting.information.capitalize %></td>
        <td><%= meeting.start_time.strftime("%H:%M") %></td>
      </tr>
    </tbody>
    <% end %>
  </table>
<% end %>