<%= calendar events: @meetings do |date, meetings| %>
  <%= l(date) %>
  <table class="table table-striped table-responsive">
    <thead>
      <th>Cliente</th>
      <th>Bairro</th>
      <th>Horário</th>
    </thead>
    <% meetings.each do |meeting| %>
    <tbody>
      <tr>
        <td><%= meeting.name.split(' ')[0] %></td>
        <td><%= meeting.district %></td>
        <td><%= meeting.start_time.strftime("%H:%M") %></td>
      </tr>
    </tbody>
    <% end %>
  </table>
<% end %>